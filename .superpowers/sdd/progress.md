# SDD Progress — Guided Trading Setup (Extension)

Plan: docs/superpowers/plans/2026-06-23-extension-guided-trading-setup.md
Mode: subagent-driven, NO COMMITS (user commits manually). Working tree only.
Baselines: /tmp/sdd/baseline-sidepanel.ts, /tmp/sdd/baseline-trading-panel.ts

## Tasks
- Task 1: setup-flow.ts — COMPLETE (8/8 tests, typecheck clean; review fixed: simplified credentials predicate, typo, test gaps)
- Task 2: setup-flow-storage.ts — COMPLETE (3/3 tests, typecheck clean; controller-reviewed, faithful to brief)
- Task 3: sidepanel hasApproval load — COMPLETE (typecheck clean, 250 tests pass; controller-reviewed vs baseline; verified response.data.allowance shape is correct)
- Task 4: portfolio-setup-view.ts — COMPLETE (4/4 tests, typecheck clean; controller-reviewed, all data-attributes match Task 5 contract)
- Task 5: sidepanel wiring — COMPLETE (typecheck clean, 254 tests pass). Reviewer caught missing Step 6 (signed-out wizard) — controller implemented + verified. Also fixed 2 pre-existing structural tests that asserted the deleted renderPortfolioTradingGate.
- Task 6: trading-panel card flow — COMPLETE (typecheck clean, 258 tests). Agent truncated early; controller finished render-switch reorder + addSetupFlow + deletions + test. Reviewer: SPEC OK, Quality Approved (3 minors).
- Task 7: styles + verification — COMPLETE (typecheck + 258 tests + production build all pass). Deviation: side panel knoww-pf-setup-* styles placed in sidepanel.ts inline <style> (correct — side panel styles its own doc inline); card knoww-tp-setup-* in knoww-inline.css. No italics. Manual browser QA still pending (user must do).

## Minor findings (for final review triage)
- Task 6: `Number(SETUP_APPROVAL_DEFAULT)` fallback is redundant (already "100"); cardSetupFlow called twice in render switch (pure/cheap); connect-case absent from addSetupFlow switch (correct, a comment would help).
- Task 5: data-setup-approve empty-input fallback hardcodes "100" instead of String(SETUP_APPROVAL_DEFAULT); harmless today (default IS 100).
- Task 5: `isDeployed: data.hasTradingWallet ? true : false` could be just `data.hasTradingWallet` (cosmetic).
- Task 4: renderSetupWizard interpolates `error` UNescaped (line ~110), whereas the old renderPortfolioTradingGate used escapeHtml(portfolioTradingError). Low risk (own error channel) but consider escaping for parity.
- Plan brief's literal `deriveSetupFlow` had a latent bug (post-current steps with a satisfied signal would render `done`, violating 'steps after now are pending'). Implementation correctly uses linear gating (`foundIncomplete`). If a later reviewer compares to the brief, this divergence is INTENTIONAL and correct.


## Final whole-feature review (opus) — RESOLVED
Verdict was Not-ready: C1 (Critical) + I1/I2 (Important). All fixed by controller + re-verified (typecheck clean, 258 tests, prod build passes):
- C1: side-panel "Add funds" called setDepositStep("method") directly, leaving portfolioFundView unset -> deposit flow half-broken. FIXED: openPortfolioFunds("deposit").
- I1: renderSetupWizard interpolated error unescaped. FIXED: self-contained escapeHtml on error.
- I2: dead "Skip for now" on signed-out connect step. FIXED: suppress skip when currentStepId==="connect"; also null portfolioOwnerAddressValue in clearPortfolioSessionState.
- Minors: approve fallback -> SETUP_APPROVAL_DEFAULT; isDeployed -> Boolean.
Reviewer confirmed architecture sound: single source of truth, order-as-gate, no resurrection, deploy-before-credentials, order-time top-up untouched.

## STATUS: ALL TASKS COMPLETE. Uncommitted (user commits manually). Manual browser QA still pending (user).

---

# SDD Progress — Extension Funding State Machine (2026-07-10)

Plan: docs/superpowers/plans/2026-07-10-extension-funding-state-machine.md
Spec: docs/superpowers/specs/2026-07-10-extension-funding-state-machine-design.md
Mode: subagent-driven, NO COMMITS (owner commits manually). Working tree only.
Baselines: scratchpad sdd/baselines/ (sidepanel.ts, trading-panel.ts, background.ts, chrome-messages.ts, portfolio-fund-intent.ts, trading-panel-ux.test.ts)

## Tasks
- Task 1: DTOs + machine + reducer tests — COMPLETE (32/32 machine tests, full suite 391/391, tsc+biome clean; review fixed BACK-from-confirm dead-end; re-review Approved). NOTE for later tasks: START carries optional address/walletMode into machine context; confirm state carries token.
- Task 2: background attempt authority — COMPLETE (10/10 store tests, full suite 401/401, tsc+biome clean; review fixed Critical terminal-guard regression [recordExecution/complete now no-op on terminal + unknown ids]; re-review Approved). Messages: KNOWW_PORTFOLIO_FUND_BEGIN_ATTEMPT / _COMPLETE_ATTEMPT; deposit/withdraw handler records txHash when msg.attemptId present.
- Task 3: gateway + controller — COMPLETE (48/48 funding tests, full 417/417, tsc+biome clean; review fixed 2 Importants: controller-local polling tracker [no machine-internal reads] + bounded give-up via new machine event CONFIRMATION_UNAVAILABLE (5 consecutive transport failures -> AMBIGUOUS_OUTCOME retryable, RETRY resumes); re-review Approved, independently re-verified).
- Task 4: sidepanel adoption — COMPLETE (429/429 full suite, tsc+biome+build clean; round 1 fixed parity regressions [in-panel Transfer Crypto cross-chain deposit restored via additive SELECT_METHOD/loadTokens source + FundingToken.chainId; withdraw quote preview restored w/ explicit confirm + BACK-first re-quote]; round 2 fixed Critical minUsd-vs-token-amount floor [FundingToken.minAmount token-unit floor via deriveDepositMinAmount]; re-review Approved). ACCEPTED deviation: awaitDepositCredit resolves credited optimistically (parity w/ baseline; follow-up).
- Task 5: trading-panel adoption + money-path switch — COMPLETE (429/429, tsc+biome+build clean; content-side WalletBridge.sendTransaction for funding = 0 across src/; round 1 fixed analytics contract [depositMethod key restored, deposit_initiated post-receipt timing] + quote preview; round 2 fixed emoji corruption + restored itemized feeBreakdown [optional FundingQuote.feeBreakdown] + deposit_initiated txHash dedupe; re-review Approved). ACCEPTED deviations: 8s optimistic credited (vs baseline 180s bridge poll, wallet method is same-chain); RETRY-resume analytics deduped.
- Task 6: cross-context test + sweep + gates — COMPLETE (cross-context 2/2 proves at-most-once across surfaces + fresh-key-after-terminal; dead manager + sidepanel dead deposit branches deleted; WalletBridge.sendTransaction 0 across src/; full ext suite 429/429, pnpm typecheck all pkgs, prod build ok; parity checklist passed; beyond-brief fix: depositErrorCopy incl. isSigningBridgeUnreachable fallback, reviewer-flagged gap fixed + controller-verified w/ dedicated test 113/113). Line counts: sidepanel 6530, trading-panel 5531, ui 5544.

## Minor findings (for final review triage)
- Task 5 (reviewer minors, deferred): amount-step preview aggregate-only (new UI, no baseline parity req); blank receive amt for non-stable token w/o quote (cosmetic).
- Task 4 (reviewer minors, deferred to Task 6/final): per-poll bridge status text not rendered in confirming; unbounded pending withdraw polling (baseline capped ~40); onFundState early-return before confirm auto-SUBMIT could stall if container/data null (near-unreachable); dead fundChainOptions/fundTokenOptions deposit branches; min-floor copy lost formatMoney ("$2" vs "$2.00"); isUsdPeggedSymbol substring heuristic.
- Task 3: passive-branch exhaustive event list in machine.test.ts drifted (missing CONFIRMATION_UNAVAILABLE; derive from Record<FundingEvent["type"],...> to force future updates); exhausted polling tracker entry lingers after give-up (inert).
- Task 2: chrome-messages walletMode typed string vs sibling TradingWalletMode (line ~278); recordExecution last-submission-wins on non-terminal txHash overwrite (noted, acceptable).
- Task 1: dead "on-chain" member in confirming.phase union (machine.ts ~116); no never-exhaustiveness guard on step switch default; validateDepositAmount assumes 1:1 USD stablecoins (documented); withdraw fetchQuote tokenDecimals:0 sentinel — Task 4 gateway must confirm it's ignored.

## Final whole-feature review (opus) — RESOLVED
Verdict: Ready for owner review. One Important fixed post-review: I1 withdraw RETRY-resume recorded the on-chain hash instead of the status handle (background.ts now records bridgeAddress ?? "direct" for withdraws, matching sidepanel-gateway's in-session handle exactly; structural test added). Also fixed: unbounded pending status polling (MAX_STATUS_PENDING_POLLS=40 → CONFIRMATION_UNAVAILABLE; 2 fake-timer tests). Controller-verified handle parity incl. direct route (no bridgeAddress returned → both sides "direct").
Final gates: extension suite 432/432 (52 files); pnpm typecheck all packages clean; production build ok (Task 6).
Documented follow-ups (non-blocking): optimistic credited on both surfaces; completeAttempt-failure + identical repeat deposit within TTL resumes silently (narrow); deposit confirm-screen asymmetry between surfaces; onFundState early-return edge; cosmetics (dead "on-chain" phase member, "$2" formatting, isUsdPeggedSymbol heuristic, per-poll status text, chrome-messages walletMode typing, passive-branch test-list drift).

## STATUS: ALL 6 TASKS + FINAL REVIEW COMPLETE. Uncommitted (owner commits manually). Manual browser QA pending (owner): wallet deposit, cross-chain Transfer Crypto (sidepanel), passive bridge address (panel), withdraw w/ quote confirm, double-click submit, SW restart mid-flow.

## 2026-07-11 — GPT 5.6 external review round (post-implementation)
5 findings verified; 4 fixed, 1 already-deferred by owner decision:
1. FIXED (Critical) stale withdraw quote: machine SET_AMOUNT/SET_DESTINATION now invalidate the pending fetchQuote correlation (invalidatePending) + clear quote; sidepanel requestWithdrawRequote dispatches SET_AMOUNT on invalid-form edits so in-flight quotes die. 3 machine tests added.
2. FIXED (Critical) account change under open panel flow: syncDepositControllerAccount in trading-panel.ts (both TradingService listeners) dispatches ACCOUNT_CHANGED when ctx.address ≠ state.corr.address, restarts deposit for the new account or exits; trading-panel gateway depositAddressesCache now keyed by proxy. New tests/funding/trading-panel-gateway.test.ts + structural test.
3. DEFERRED (Required) eager loading / module decomposition — the second sub-project the owner scoped out ("funding first, then lazy-load"); unchanged.
4. FIXED (Required) gateway duplication: new funding/gateways/shared.ts (RuntimeResponse, executionError, toFundingAttempt, toDecimalString, deriveDepositMinAmount); both gateways import it; cross-surface import removed; test import repointed.
5. FIXED (Required) unbound attempt updates: store recordExecution/complete now require expectedIdempotencyKey (mismatch → no-op + logWarn); COMPLETE_ATTEMPT message carries idempotencyKey (validated in background); both gateways send it. 2 mismatch tests added.
Gates: extension vitest 440/440 (53 files), pnpm typecheck all packages clean. Still uncommitted (owner commits manually).
- Extension lazy loading Phase 1 Task 1: canonical adapter exports, file-aware platform manifest, and typed matcher wrapper complete; focused 61/61, full extension 501/501, and tsc clean.
- Extension lazy loading Phase 1 Task 2: dual classic/ESM webpack configs, manifest-driven 50-platform entries, WAR coverage, and complete stats artifacts; isolated dev+prod builds and production assertion passed, full extension 501/501, and tsc clean.
- Extension lazy loading Phase 1 Task 3: core-owned manifest dispatcher and awaited startup handoff complete; 50 adapter self-registration paths and index imports removed; focused 68/68, full extension 508/508, tsc, and isolated dev webpack green.
- Extension lazy loading Phase 1 Task 4: recursive entrypoint stats traversal, committed routing fixtures, exact bundle/graph assertions, and disabled Stack Exchange reachability cleanup complete; focused 72/72, full extension 512/512, tsc clean, isolated production build/assert green at 49 platform entries/assets, and bogus-entry negative control rejected as expected.
- Extension lazy loading Phase 1 Task 5: dependency-free built-ESM smoke harness complete with fresh-process isolation for all 49 manifest adapters and a temporary negative side-effect control; isolated production build/assert/smoke green, full extension 516/516, tsc and scoped Biome clean; no adapter source changes required.
- Extension lazy loading Phase 2 Task 6: complete pure stream-bet and Decimal.js outcome-balance closures moved verbatim into `src/content/ui/` with importers/tests re-pointed and no compatibility shims; RED missing-module proof, focused 38/38, full extension 516/516, tsc clean, structural old-path/file checks clean, and scoped Biome clean with assists disabled to preserve path-only ownership in dirty shared files.
- Extension lazy loading Phase 2 Task 7: legacy `src/content/ui.ts` statically decomposed into cards, notifications, stream shell, trading glue, and index ownership modules with required synchronous ports, one combined message listener, and the exact ordered 16-member `KNOWW_UI` surface; RED structural proof, focused 130/130, full extension 522/522, tsc and scoped Biome clean, and isolated production build/assert/49-module smoke green.
- Extension lazy loading Phase 2 Task 8: core-safe `TradingRuntime` facade and inert `trading-entry` factory complete; bridge/service/signing side effects now have retained-callback installers/disposers, accepted signing work receives one correlated disposal error and cannot complete stale after recreation, WalletConnect shared state is acquired lazily, inline deposits close during runtime teardown, runtime creation is cached while alive and recreatable after idempotent disposal, UI boot still installs eagerly through the facade, and stream hydration returns a real teardown handle; RED 4/4 proof, focused lifecycle suites green, full extension 531/531, real-entry import-purity instrumentation, tsc, scoped Biome, and diff-check all green.
- Extension lazy loading Phase 2 Task 9: retryable single-flight trading loader with safe eager-runtime adoption, exact-true warm prefetch flag lifecycle, single core portfolio/signing dispatcher, observable signing/cancel lifecycles, and generation-owned WalletConnect transitions complete; selected cancelled-as-idle precedence, cancel-during-import, correlated signing failure, stale completion/failure guards, composed core barriers, pinned-provider quarantine that serializes every cancel/force-new caller behind the captured old approval and its topic cleanup, retryable exact stale-session/topic descriptors after cleanup failure, missing-topic safe blocking, identity/topic-scoped teardown, and established/newer-session preservation covered; RED 3-suite/module proof plus composed-clear, failed-abort, stale-success, delayed stale-topic cleanup, pinned no-op-abort serialization, and retained-descriptor retry race proofs, focused 58/58 across Task 9/review suites, full extension 582/582, tsc, scoped Biome, and diff-check green (production build intentionally deferred to isolated Task 9 runner).
- Extension lazy loading Phase 2 Task 10: `content-trading.js` ESM/WAR emission, zero-eager core cutover, loader-owned card intents, per-widget lazy stream hydration/disposal, host-owned inline-deposit cleanup/collapse, and cancellation-aware first-mounted-feed-card warm prefetch complete; initial RED 7-boundary proof plus review RED for queued-idle pagehide, focused review suite 27/27, full extension 599/599, tsc, scoped Biome, staged/unstaged diff-check, isolated production MultiCompiler, exact 50-entry/asset assertion, and 49-adapter smoke green.
- Extension lazy loading Phase 2 Task 11: fail-closed static ESM export parsing, recursive classic/trading graph boundaries, exact canonical lazy-WAR ownership/matches, exact 309,138-byte core budget (281,034-byte Task 10 baseline + 10%), TypeScript-AST core dependency ban (value imports/re-exports/requires/dynamic imports; unresolved forms fail closed except exact loader contracts), and isolated two-phase `content-trading.js` lifecycle smoke complete; loader exceptions use AST/token structure, exact resource literal contents, and exact `/* webpackIgnore: true */` text so whitespace inside tokens/comments cannot collide. The smoke found and drove removal of real import-time `KNOWW_UTILS` and React DOM feature-probe effects via a pure HTML-escape leaf and render-local literal webpack requires. Built lifecycle proof rejects a signing-role Chrome callback and observes exactly one bridge-to-service account subscription plus exact once-only cleanup. RED missing-helper/eager-QR/dependency-bypass/dynamic-import/WAR-helper/fingerprint-collision proofs, focused 84/84, full extension 611/611, tsc and scoped Biome clean; isolated production MultiCompiler + assertion green with `content.js` at 281,042 bytes, 49 adapter smokes + trading import/factory/idempotent-dispose green, and 9 negative controls rejected. Phase 2 complete; uncommitted for owner.
- Extension lazy loading Phase 3 Task 12: trading-panel mutable state consolidated behind the stable live `panelState` object (including lifecycle, order/setup/funding/session/timer/cache/overflow fields); 16 dependency-closed formatters and the wallet/setup/banner views extracted into `panel/{format,panel-state,setup-view}.ts`, with setup DOM dependencies passed through `SetupViewUiPort` and banner dismissal ordering preserved. RED missing-module proof 3/3, focused 62/62, full extension 615/615, tsc and scoped Biome clean; `trading-panel.ts` reduced from 5,579 to 5,228 lines; no staging or commits.
- Extension lazy loading Phase 3 Task 13: positions, deposit/funding, and order closures extracted into `panel/{positions-view,deposit-view,order-view}.ts` with explicit UI ports, one-way dependencies, host-identity-safe inline deposit teardown, late-listener guards, and no shell back-imports or duplicate owners. RED missing-view proof, focused UX/lazy/stream/funding/structure suites 153/153, full extension 616/616, tsc, scoped Biome, ownership/cycle audits, and diff-check green; `trading-panel.ts` reduced from 5,228 to 1,330 lines; no webpack build (deferred to Task 14), staging, or commits.
- Extension lazy loading Phase 3 Task 14: `trading-panel.ts` finalized as the 1,330-line singleton lifecycle/mount/render router delegating to all six panel modules; production stats now fail closed unless every panel module remains in the `content-trading` graph. RED missing-graph-proof captured; real extracted deposit-view coverage proves B-replaces-A, stale A callbacks cannot disturb B, and each host unsubscribe/onClose fires exactly once. Focused 155/155, full extension 618/618, tsc, scoped Biome, and diff-check green. Fresh isolated `/tmp` production MultiCompiler build succeeded; assertion passed with exactly 49 platform entries plus `content-trading`, `content.js` 281,042 bytes within its 309,138-byte budget, exact one-export/WAR/graph contracts, and smoke passed all 49 adapters plus the two-phase trading lifecycle while rejecting all 9 controls. Phase 3 complete; no staging or commits.
- Extension lazy loading Phase 4 Task 15: sidepanel cross-section types/escaping/Decimal display/view switching extracted to `sidepanel/shared.ts`; exact background RPC, fetch, wallet-mode storage, requested-view consumption, and the sole retained-callback runtime listener extracted inertly to `sidepanel/messaging.ts`; snapshot decoding/rendering, market markup/styles, stale-safe 2-character/300ms search, row/focus/new-tab interactions, persist→show-page→close surface switching, and one owned 5-second refresh lifecycle extracted to `sidepanel/markets.ts`. RED missing-module proof; behavior tests cover lastError/null/synchronous-throw baseline semantics, storage fallback, all listener routes/removal order, escaping/section order, stale search rejection, exact one-interval disposal, minimize a11y state, and floating-surface ordering. Focused 47/47, full extension 625/625, tsc, scoped Biome, ownership audit, and diff-check green; `sidepanel.ts` reduced from 6,538 to 5,851 lines (`shared` 118, `messaging` 172, `markets` 370). Build deferred to Task 16; no staging or commits.
- Extension lazy loading Phase 4 Task 16: `sidepanel.ts` is now a 191-line direct composer for the setup, funding, portfolio, markets, and messaging factories/lifecycles; setup/session/WalletConnect/deployment/approval/degraded-read trust moved to `sidepanel/setup.ts` (1,639 lines), controller/gateway/Decimal quote and deposit/withdraw rendering/events moved to `sidepanel/funding-ui.ts` (1,985), and portfolio fetching/generation guards/positions/orders/history/sell/cancel/30-second refresh moved to `sidepanel/portfolio.ts` (2,408). Each module owns its CSS constant and mutable state behind a factory; sibling construction and shell/market lifecycle stay in the entry. Review fixes add generation/owner-guarded, fully serialized read→decide→write setup reconciliation (including persisted-true/live-complete preservation and same-owner delayed-write correction) plus one named entry-owned root click/change/input dispatcher set with exact disposal/restart. Click staging is market-first, then the portfolio cancel-disarm/action pre-pass, common settings/tabs, setup, and funding; portfolio disposal also clears any armed cancel timer/button. RED missing-module/direct-composition plus cross-owner A/B, persisted-live-complete, same-owner delayed-write, listener count/order/dispose/restart, and real armed-cancel settings/disposal proofs; focused sidepanel/funding suites 174/174, full extension 631/631 across 74 files, tsc, scoped Biome, and diff-check green. Fresh isolated `/tmp` production MultiCompiler build succeeded; assertion passed with 49 platform entries, `content.js` 281,042 bytes within the 309,138-byte budget, and ESM smoke passed all 49 adapters plus trading lifecycle while rejecting 9 controls. Phase 4 complete; no staging or commits.
- Extension lazy loading final whole-feature review: fail-closed platform startup now stops before enablement, analytics, or UI boot when the manifest dispatcher cannot load an adapter or detection yields no platform. Each content bridge runtime latches one non-empty nonce: init captures an existing nonce, early init rejects missing/wrong nonces until the first valid current nonce arrives, later global nonce rotations cannot activate the stale listener, and disposal resets the latch so a recreated runtime can bind the replacement nonce. Injected bridge scripts issue one nonce-tagged wallet-discovery request on load for late page-bridge initialization. The isolated ESM smoke fixture supplies the same nonce contract without weakening listener, subscription, or disposal assertions. Initial review RED proofs failed 4/4 before implementation; the final nonce-rotation regression also failed exactly before the latch fix, then the focused lifecycle/styles/runtime set passed 10/10. Broader related tests pass 51/51, and the full extension suite passes 634/634 across 76 files. TypeScript and scoped Biome are clean. Fresh secret-excluding isolated `/tmp` production MultiCompiler build and exact assertion pass with 49 platform entries and `content.js` at 281,326 bytes within the 309,138-byte budget; ESM smoke passes all 49 adapters plus the two-phase trading lifecycle while rejecting all 9 controls. No staging or commits.
- Extension lazy loading final measurements/handoff: `content.js` reduced from the 1.44 MB pre-project baseline to 281,326 bytes. Lazy output is `content-trading.js` at 838,491 bytes plus 49 isolated `platforms/*.js` assets totaling 405,034 bytes (largest 32,048 bytes), with names owned by the canonical platform manifest; classic `sidepanel.js` is 198,629 bytes. Former giants: `sidepanel.ts` 6,538 → 191 lines (`shared` 118, `messaging` 172, `markets` 370, `setup` 1,639, `funding-ui` 1,985, `portfolio` 2,408); `trading-panel.ts` 5,579 → 1,330 lines (six panel modules own state/format/setup/positions/deposit/order); legacy `ui.ts` 5,544 → deleted (`cards` 1,499, `notifications` 2,892, `stream-bet-ui` 965, `index` 73, with pure/message leaves separate). Final independent review: approved with no Critical/Important findings; targeted re-review 85/85, full extension 634/634, tsc/Biome/diff, fresh isolated production build/assert, and 49+trading smoke with 9 rejected controls all green. Everything remains uncommitted for owner review; manual browser QA and before/after parse/execute timing capture remain pending.
