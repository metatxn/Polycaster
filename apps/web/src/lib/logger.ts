/**
 * Re-export of the shared `@knoww/logger` for compatibility with existing
 * `import { logger } from "@/lib/logger"` callers. Prefer importing from
 * `@knoww/logger` directly in new code.
 */
export { logger } from "@knoww/logger";
